---
phase: 1
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/page.tsx
  - src/components/dashboard/lead-workspace.tsx
  - src/components/dashboard/lead-preview-dialog.tsx
  - app/globals.css
requirements:
  - ACCESS-01
  - LEAD-03
  - PACK-03
requirements_addressed:
  - ACCESS-01
  - LEAD-03
  - PACK-03
must_haves:
  truths:
    - "D-01: slash route is the primary publishable route"
    - "D-02: missing assistant credentials do not block dashboard"
    - "D-03: crashes and embarrassing first-screen states are fixed before polish"
    - "D-08: existing preview and status surfaces prove the workflow"
    - "D-09: lead review remains human-controlled with explicit uncertainty"
    - "D-10: review/status loop takes priority over new intake scope"
    - "D-11: first viewport immediately communicates lead operations"
    - "D-12: visual blockers for outreach screenshots are fixed"
    - "D-13: preserve dense console direction instead of redesigning"
autonomous: true
---

# Plan 02: Harden Main Console Review Flow

<objective>
Ensure the `/` dashboard route opens cleanly, presents a professional first viewport, and proves one visible lead preview plus review/status update path.
</objective>

<must_haves>
<truths>
- D-01: `/` is the primary publishable route for Phase 1.
- D-02: missing assistant credentials must not block the non-assistant dashboard.
- D-08: console lead rows use the existing reusable preview and existing review/status actions.
- D-11: first viewport communicates lead operations immediately.
- D-12: fix clipped dialogs, unreadable dark-mode surfaces, overlapping text, sparse states, and broken navigation.
- D-13: preserve the dense operational console baseline.
</truths>
</must_haves>

<tasks>
<task id="01-02-T1" type="execute">
<read_first>
- `.planning/phases/01-publishable-lead-review-flow/01-CONTEXT.md`
- `.planning/phases/01-publishable-lead-review-flow/01-UI-SPEC.md`
- `app/page.tsx`
- `src/components/dashboard/app-shell.tsx`
- `src/components/dashboard/lead-workspace.tsx`
- `src/components/dashboard/lead-preview-dialog.tsx`
</read_first>
<action>
Verify and harden the `/` route composition so missing or invalid `leadId`, empty source definitions, empty custom fields, empty lead rows, and disabled assistant runtime render controlled states instead of crashing. Keep `AppShell` as the shell and keep `LeadPreviewDialog` controlled by `leadId` query state.
</action>
<acceptance_criteria>
- `app/page.tsx` guards invalid `leadId` values and renders without a preview when no matching lead detail exists.
- The console renders a clear empty state when `leadRows.length === 0`.
- Assistant disabled state does not remove the shell, nav, search, theme toggle, or New intake action.
- `npm run typecheck` exits 0 after changes.
</acceptance_criteria>
</task>

<task id="01-02-T2" type="execute">
<read_first>
- `src/components/dashboard/lead-workspace.tsx`
- `src/components/dashboard/lead-preview-dialog.tsx`
- `src/components/dashboard/lead-ui.tsx`
- `app/globals.css`
</read_first>
<action>
Tune the first viewport and preview for publication readiness without redesigning the IA. Ensure metrics, queues, filters, and lead ledger are visible on desktop; ensure the preview close button and open-full-page/review/status controls remain visible and reachable on desktop and mobile; ensure dark-mode tokens are readable for ledger rows, preview dialog, badges, and form controls.
</action>
<acceptance_criteria>
- Desktop `/` first viewport includes the operational metrics strip and lead ledger above the fold at common laptop width.
- `.ops-preview-dialog` and its shell have responsive max sizes or overflow behavior that prevents clipped close/action controls.
- Dark-mode preview, ledger, and status badge text use existing CSS variables or explicit dark-safe classes.
- No new card-inside-card layout is introduced.
</acceptance_criteria>
</task>

<task id="01-02-T3" type="execute">
<read_first>
- `src/components/dashboard/lead-workspace.tsx`
- `src/components/leads/lead-status-form.tsx`
- `src/components/leads/lead-detail-form.tsx`
- `app/api/leads/[leadId]/status/route.ts`
- `src/lib/domain/leads/change-lead-status.ts`
</read_first>
<action>
Confirm the preview or full lead path exposes at least one visible status or review action backed by existing domain/API behavior. If the action is hidden, broken, or only available too deep in the full page, adjust the preview/full-page action placement using existing components and routes.
</action>
<acceptance_criteria>
- A user can open a lead from the console and reach a visible status or review action without losing route context.
- Status mutation still calls the existing lead status route or domain function path.
- No assistant-only mutation is required to satisfy LEAD-03.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run typecheck`.
- Start the app if prerequisites are available and verify `/` opens without a crash.
- Open a seeded lead preview, close it, and confirm the URL no longer contains `leadId`.
- Trigger one review/status update and confirm the UI or persisted state changes.
</verification>

<success_criteria>
- ACCESS-01, LEAD-03, and PACK-03 are satisfied by the existing console flow.
- The first viewport is credible for outreach and does not depend on optional assistant credentials.
</success_criteria>
