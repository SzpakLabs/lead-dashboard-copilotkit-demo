---
phase: 1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/seed.ts
requirements:
  - SAFE-01
  - LEAD-01
  - LEAD-02
requirements_addressed:
  - SAFE-01
  - LEAD-01
  - LEAD-02
must_haves:
  truths:
    - "D-04: deterministic four-month seed data expansion"
    - "D-05: all person names in test/demo input include Test"
    - "D-06: seed remains idempotent and uses workspace source definitions"
    - "D-07: representative leads expose operational fields and activity"
autonomous: true
---

# Plan 01: Expand Safe Demo Data

<objective>
Create deterministic, idempotent demo seed data that makes the main lead ledger look populated and representative while preserving the mandatory Test-name safety rule.
</objective>

<must_haves>
<truths>

- D-04: seed data covers roughly two months before and two months after a deterministic anchor.
- D-05: every person name used in seed data contains `Test`.
- D-06: seed reset remains idempotent for `software-services-demo`.
- D-07: representative leads expose budget, urgency or timeline, service type, source, status, next step, and follow-up timing where available.
  </truths>
  </must_haves>

<tasks>
<task id="01-01-T1" type="execute">
<read_first>
- `.planning/phases/01-publishable-lead-review-flow/01-CONTEXT.md`
- `.planning/phases/01-publishable-lead-review-flow/01-RESEARCH.md`
- `src/lib/db/seed.ts`
- `src/lib/db/schema.ts`
- `src/lib/domain/leads/status.ts`
- `src/lib/domain/sources/manage-sources.ts`
</read_first>
<action>
Update `src/lib/db/seed.ts` to use a deterministic seed anchor constant or `DEMO_SEED_ANCHOR` fallback. Keep `demoWorkspaceSlug = "software-services-demo"`. Preserve reset behavior that deletes the existing workspace by slug before reseeding. Generate a reusable set of source definitions, contacts, ingestion events, leads, custom fields, custom field values, follow-ups, and lead events around the anchor date. Use configured source slugs such as `linkedin`, `upwork`, `referral`, `website`, and `other`.
</action>
<acceptance_criteria>
- `src/lib/db/seed.ts` contains `DEMO_SEED_ANCHOR` or an equivalent deterministic anchor constant.
- `src/lib/db/seed.ts` still deletes `workspaces` where slug equals `software-services-demo` when `--reset` is passed.
- `src/lib/db/seed.ts` inserts more than 20 leads or creates lead fixtures from an array with more than 20 entries.
- `src/lib/db/seed.ts` uses source definition slugs that are inserted for the workspace.
</acceptance_criteria>
</task>

<task id="01-01-T2" type="execute">
<read_first>
- `src/lib/db/seed.ts`
- `.kiro/specs/lead-workflow-ux-upgrade/tasks.md`
- `.kiro/specs/lead-workflow-ux-upgrade/requirements.md`
</read_first>
<action>
Populate the seed fixtures with varied statuses (`needs_review`, `contacted`, `scheduled`, `in_progress`, `won`, `lost` where supported), budgets, timelines, next steps, review confidence values, missing fields, scheduled dates, follow-up due dates, source artifacts, custom field values, and lead events. Include overdue, upcoming, scheduled, completed, won, and lost examples. Ensure several dates share multiple lead-related events for later calendar stacking checks.
</action>
<acceptance_criteria>
- Seed fixtures include at least five distinct lead statuses supported by `leadStatusSchema`.
- Seed fixtures include at least five distinct source slugs.
- Seed fixtures include budget strings, timeline values, `nextStep`, `scheduledAt`, and `followUpDueAt` across representative leads.
- Seed fixtures include `missingFields` on at least one needs-review lead.
- Seed follow-up or scheduled-work dates include at least three dates with multiple events.
</acceptance_criteria>
</task>

<task id="01-01-T3" type="execute">
<read_first>
- `src/lib/db/seed.ts`
- `/Users/macos/.codex/agent-rules/commit-messages.md`
</read_first>
<action>
Audit all seeded person names, owner names, contact names, raw text, normalized text, follow-up notes, and activity summaries. Replace any realistic non-test person name with a name containing `Test`, for example `Maria Test`, `Carlos Test`, `Alex Test`, or `Owner Test`.
</action>
<acceptance_criteria>
- Searching `src/lib/db/seed.ts` finds no person-like seeded names that lack `Test`.
- `src/lib/db/seed.ts` contains multiple Test-safe names, including owner and contact examples.
- No browser, chat, form, or documentation examples are added with realistic non-test names.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run typecheck`.
- If `DATABASE_URL` is configured, run `npm run db:reset-demo`.
- Inspect `src/lib/db/seed.ts` for Test-name safety and deterministic date generation.
</verification>

<success_criteria>

- Demo seed data is dense, deterministic, idempotent, and test-safe.
- The default dashboard has enough lead variety to satisfy LEAD-01 and LEAD-02.
  </success_criteria>
