---
phase: 2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/lead-preview-dialog.tsx
  - src/components/dashboard/lead-workspace.tsx
  - src/components/leads/lead-detail-tabs.tsx
  - app/calendar/page.tsx
  - app/settings/page.tsx
  - app/settings/fields/page.tsx
  - app/settings/sources/page.tsx
  - src/components/dashboard/global-lead-search.tsx
  - src/components/dashboard/lead-search-overlay.tsx
  - src/components/dashboard/dense-mode-toggle.tsx
  - src/components/dashboard/theme-toggle.tsx
  - app/globals.css
requirements:
  - LEAD-04
  - LEAD-05
requirements_addressed:
  - LEAD-04
  - LEAD-05
must_haves:
  truths:
    - "D-01: slash route remains primary demo entry"
    - "D-05: shared lead preview is used for console, calendar, and search"
    - "D-06: preview stays concise with summary, status, source, next step, follow-up, and actions"
    - "D-07: full lead detail remains the focused tabbed workspace"
    - "D-08: preview open close and full-page route context are verified on desktop and mobile"
    - "D-09: calendar explains dashboard-only internal lead workload"
    - "D-13: seeded data density remains the source of calendar/report confidence"
    - "D-19: phase is verification and hardening around active-spec work"
    - "D-20: browser checks cover calendar preview, lead tabs, settings, search, dense mode, dark mode, and clipping"
    - "D-22: browser checks use Test-safe names"
autonomous: true
---

# Plan 01: Harden Supporting Inspection Surfaces

<objective>
Verify and fix the shared lead preview, full lead detail tabs, calendar, settings, search, dense mode, and dark mode so supporting surfaces let users inspect leads without losing workflow context.
</objective>

<must_haves>
<truths>

- D-05: shared preview remains the quick-inspection surface for console, calendar, and search.
- D-07: full lead detail remains the deeper tabbed workspace.
- D-09: calendar stays limited to internal dashboard lead/follow-up data.
- D-20: targeted browser checks cover the supporting surfaces named in the phase.
  </truths>
  </must_haves>

<threat_model>

- T-02-01: A `leadId` query value could open or leak an unrelated lead. Mitigation: preserve workspace-scoped data loading and only render preview content for matching demo workspace records.
- T-02-02: Tab or preview edits could bypass domain routes. Mitigation: preserve existing form/API/domain mutation paths and do not add component-level database writes.
- T-02-03: Calendar wording could imply external availability. Mitigation: keep calendar behavior and copy framed as internal dashboard data only.
  </threat_model>

<tasks>
<task id="02-01-T1" type="execute">
<read_first>
- `.planning/phases/02-supporting-ops-surfaces/02-CONTEXT.md`
- `.planning/phases/02-supporting-ops-surfaces/02-UI-SPEC.md`
- `.planning/phases/02-supporting-ops-surfaces/02-PATTERNS.md`
- `src/components/dashboard/lead-preview-dialog.tsx`
- `src/components/dashboard/lead-workspace.tsx`
- `src/components/dashboard/global-lead-search.tsx`
- `src/components/dashboard/lead-search-overlay.tsx`
- `app/calendar/page.tsx`
</read_first>
<action>
Verify that console rows, calendar events, and global search results all use the same `leadId` preview route contract. If any path navigates directly away or drops existing query context, adjust that path to open `LeadPreviewDialog` through `leadId` while preserving unrelated query parameters and `scroll: false`.
</action>
<acceptance_criteria>
- Console lead selection opens `LeadPreviewDialog`.
- Calendar lead or follow-up event selection opens `LeadPreviewDialog` instead of immediately navigating to `/leads/[leadId]`.
- Search result selection opens `LeadPreviewDialog`.
- Closing preview removes `leadId` from the URL and leaves other query parameters intact.
- `npm run typecheck` exits 0.
</acceptance_criteria>
</task>

<task id="02-01-T2" type="execute">
<read_first>
- `.planning/phases/02-supporting-ops-surfaces/02-UI-SPEC.md`
- `src/components/leads/lead-detail-tabs.tsx`
- `src/components/leads/lead-detail-form.tsx`
- `src/components/leads/lead-status-form.tsx`
- `src/components/leads/follow-ups-panel.tsx`
- `src/components/leads/custom-field-values-form.tsx`
- `app/leads/[leadId]/page.tsx`
</read_first>
<action>
Verify the full lead page tabs `Overview`, `Review`, `Follow-ups`, `Source`, `Activity`, `Custom fields`, and `Full view`. If a tab shows unrelated controls, loses existing behavior, breaks hash sync, or overlaps on mobile, adjust the current tab component and CSS without adding a new detail surface.
</action>
<acceptance_criteria>
- `src/components/leads/lead-detail-tabs.tsx` still declares all seven required tab labels.
- ArrowLeft, ArrowRight, Home, and End keyboard handling remains present for tab navigation.
- Editable fields appear only in the relevant tab panels.
- `Full view` remains read-only and hides empty fields where practical.
- `npm run typecheck` exits 0.
</acceptance_criteria>
</task>

<task id="02-01-T3" type="execute">
<read_first>
- `.planning/phases/02-supporting-ops-surfaces/02-UI-SPEC.md`
- `app/calendar/page.tsx`
- `app/settings/page.tsx`
- `app/settings/fields/page.tsx`
- `app/settings/sources/page.tsx`
- `src/components/leads/source-definitions-panel.tsx`
- `src/components/leads/custom-field-definitions-panel.tsx`
- `src/components/dashboard/dense-mode-toggle.tsx`
- `src/components/dashboard/theme-toggle.tsx`
- `app/globals.css`
</read_first>
<action>
Verify and harden calendar stacking, settings source/custom-field surfaces, dense mode, and dark mode readability. Fix clipped controls, overlapping labels, non-persistent toggle behavior, or unreadable colors by reusing existing `ops-*` CSS variables and local component patterns.
</action>
<acceptance_criteria>
- Calendar days with multiple events remain readable and clickable.
- Settings contains usable `Custom fields` and `Sources` sections.
- Source controls support enable or disable, add, rename, archive, and order behavior where the existing UI exposes those actions.
- Dense mode visibly changes ledger row height, padding, or secondary text density and persists.
- Dark mode persists after reload and keeps console, calendar, settings, preview, and lead detail readable.
- No new one-off dark palette is added outside existing CSS token patterns.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run typecheck`.
- If prerequisites are available, run `npm run check`.
- Browser-check desktop and mobile paths for console preview, calendar preview, search preview, lead detail tabs, settings, dense mode, and dark mode.
</verification>

<success_criteria>

- LEAD-04 is satisfied by shared preview and tabbed full detail inspection.
- LEAD-05 is supported by credible calendar and operational surfaces.
  </success_criteria>
